# Workflow Reference

## Idea To Chapter

Idea -> concept board -> outline -> character/canon constraints -> chapter brief -> AI Novel generation -> user approval -> knowledge update.

## Failure Triage

Backend -> provider/model route -> structured output -> task state -> logs -> database.

## Useful CLI

```bash
python scripts/lab_doctor.py --strict
python mcp-bridge/ai_novel_mcp.py list-tools
python mcp-bridge/ai_novel_mcp.py healthcheck
python mcp-bridge/ai_novel_mcp.py bootstrap
```
